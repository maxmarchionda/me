# Server-only secrets — set these in Vercel Project Settings → Environment Variables.
# NEVER prefix these with VITE_ (that would inline them into the client bundle).
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REFRESH_TOKEN=
